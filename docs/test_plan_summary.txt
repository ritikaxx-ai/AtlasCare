AtlasCare — Test Plan Summary
================================
Version 3.0 | LangGraph + Groq (LLaMA 3.3 70B)

COVERED
-------
1. Unit tests (tests/test_agent.py)
   - J-GREET: greeting fast-path, 0 LLM calls, < 50ms
   - J1: get_order_status, tracking in response
   - J2: cancel_order_item before execute_refund (order enforced)
   - J2: cancel_full_order for "cancel everything"
   - J2: update_shipping_address with saved label
   - J3: guardrail blocks execute_refund; CRM case created with trace_id
   - J4: get_customer_interaction_history → ChromaDB RAG
   - J5: get_case_status for CASE-XXXXXX queries
   - J-KB: search_kb for policy/return/warranty questions
   - J-BLOCKED: prompt injection → blocked, 0 LLM calls
   - Multi-turn: session memory resolves order_id across turns

2. E2E latency (tests/test_latency_e2e.py)
   - All journeys against live POST /query when server running
   - Greeting < 50ms, J1/J2/J3 < 2s P95

3. Safety
   - Rs.42000 / ₹42,000 / "42000 rupees" formats all trigger J3 escalation
   - execute_refund never fires when amount > ₹25,000
   - Prompt injection patterns blocked pre-LLM (0 Groq calls wasted)
   - Unauthorized order access rejected (customer_id mismatch)
   - Executor gate: cancel soft-failure (already_cancelled, not_found) blocks execute_refund
   - Phantom refund prevention verified: already-cancelled item → 0 refund calls

4. Session Memory
   - Turn 1 establishes order_id, Turn 2 resolves it from memory
   - Last 10 turns passed to Groq for reference resolution

5. API contract
   - POST /query — message + session_id → response + trace + journey_type
   - GET /health — 200 OK
   - GET /metrics — LLM stats, journey counts, guardrail triggers

6. Docker
   - Image builds successfully (183MB)
   - Container starts, health check passes
   - All API endpoints functional inside container

OUT OF SCOPE (this submission)
------------------------------
- Load test at 18K requests/day
- Auth, rate limiting, production database
- End-to-end LLM regression suite (golden set)
- ChromaDB cross-customer isolation stress test

BEFORE PRODUCTION
-----------------
- Expand mock data beyond 3 sample orders
- Golden-set LLM regression tests for edge-case phrasings
- SRE runbooks + alert thresholds on /metrics
- Compliance audit export (traces + cases to S3/GCS)
- Rate limiting per customer_id
- JWT auth on /query endpoint

RUN
---
  # Docker (recommended)
  docker-compose up
  curl http://localhost:8000/health

  # Local
  source venv/bin/activate
  uvicorn main:app --host 0.0.0.0 --port 8000 --reload
  pytest tests/ -v
